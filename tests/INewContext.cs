namespace Chatter.Testing.Core
{
    /// <summary>
    /// This is a marker interface used as part of the test context to permit extension creators to attach to for
    /// the creation of rest objects
    /// </summary>
    public interface INewContext
    {
    }
}
